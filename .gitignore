# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtualenv
.venv/
venv/
env/

# Playwright / browser cache
.cache/
ms-playwright/

# Runtime artifacts generated by the crawler
# (regenerated on every run; not source)
datasets/.dummy_uploads/
datasets/.crawler_notes.json
.dummy_uploads/
.crawler_notes.json

# Generated datasets (keep the folder, ignore large outputs).
# Comment these out if you want to version specific reference datasets.
datasets/*.jsonl
datasets/*.csv

# Exploratory / smoke outputs
archive/smoke/*.jsonl
archive/smoke/*.csv

# OS noise
.DS_Store
Thumbs.db
